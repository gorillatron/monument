<table class="table">
  <thead>
    <tr>
      <th>Name</th>
      <th>Email</th>
      <th>Phone</th>
      <th></th>
    </tr>
  </thead>
  <tbody>

    {{#each user in model}}
      <tr>
        <td>
          {{#link-to 'users.find' user}} {{user.name}} {{/link-to}}
        </td>
        <td>{{user.phoneNumber}}</td>
        <td>{{user.email}}</td>
        <td>
          <button type="button" class="btn btn-danger" {{action 'destroy' user}}>delete</button>
        </td>
      </tr>
    {{/each}}

  </tbody>
</table>
